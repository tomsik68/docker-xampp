# Default values for Xampp.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

xamppnameOverride: xampp
xamppfullnameOverride: xampp

environment: dev

xampp:
  enabled: true
  containers:
    replicaCount: 0
    containerPort: 80
    image:
      repository: 'registry.com/xampp/xampp'
      pullPolicy: Always
      tag: demo
    command: ""
  pipelinedeployed: ""
  # If service.type eq NodePort then specify static service.nodePort
  service:
    create: true
    type: NodePort # ClusterIP
    serviceApplicationPort: 80
    serviceApplicationnodePort: 41062
    serviceSFTPPort: 22
    serviceSFTPnodePort: 41060
    serviceFTPPort: 21
    serviceSTPnodePort: 41061
    serviceMySQLPort: 3306
    serviceMySQLnodePort: 41063
  ingress:
    enabled: false
  ingressRoute:
    enabled: false



deployment:
  additionalLabels:
    owner: xampp

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations:
  # include Gitlab CI annotations for integration with CI deployment


podSecurityContext: {}
securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000



resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
